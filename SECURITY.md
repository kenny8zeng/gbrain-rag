# 安全策略

## 支持版本

| 版本 | 支持状态 |
|---|---|
| main（最新） | ✅ 积极维护 |
| 已发布 tag | ✅ 按发布公告维护 |

## 报告漏洞

请**不要**在公开 issue 中报告安全漏洞。

- 优先渠道：GitHub Security Advisory（仓库 → Security → Report a vulnerability）
- 备用渠道：私信项目维护者

请包含：

1. 受影响版本与复现步骤
2. 漏洞影响评估（可被谁利用、造成什么后果）
3. 可选的修复建议

维护者将在 7 天内确认并评估；修复前漏洞细节保持私密，修复发布后公开致谢。

## 已知安全姿态

- URL 导入不做地址校验（SSRF 风险由调用方与部署边界承担）——部署必须隔离受信环境
- 托管 OCR（anydoc）与外部解析服务（docling）可能使文档离开本机——默认关闭/按配置
- 管理面鉴权：单一 `ADMIN_TOKEN`；租户面：分区级 API key（哈希存储、明文仅签发一次）
